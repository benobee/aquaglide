//import controller from "../core/controller";

/**
 * abstracted out for readability
 * @example
 * controller.on("navbar", (el) => {
 *   navbar.init();
 * });
 */

const api = () => {

};

export default api;